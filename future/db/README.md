# Future Database Prototype

Reserved for future database schema and migration experiments.

Start with SQLite for local/server testing. Move to PostgreSQL only if needed for multi-user deployment.

The database must preserve daily/versioned snapshots and never bypass validation before publishing active portal data.
