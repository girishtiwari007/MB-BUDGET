# Future API Prototype

Reserved for a future backend API.

Purpose:

- receive uploads
- validate source files
- stage parsed data
- confirm and publish active payloads
- refresh exports
- record audit logs

This must reuse the existing validated parser/export rules before replacing any current workflow.
